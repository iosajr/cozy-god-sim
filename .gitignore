# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# System / editor cruft
.DS_Store
Thumbs.db
*.tmp

# Claude Code local settings (keep shared skills, ignore personal settings)
.claude/settings.local.json

# Scratch git worktrees created for isolated agent sessions
.claude/worktrees/

# Request/ (villager-idea pipeline, see Request/README.md): generated/
# personal state, not shared -- game_state.example.json IS committed.
Request/__pycache__/
Request/*.pyc
Request/game_state.json
Request/review_queue.jsonl
